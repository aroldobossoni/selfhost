---
profile: production

skip_list:
  - galaxy[no-changelog]
  - var-naming[no-role-prefix]  # Variables intentionally use proxmox_, vm_, etc. for clarity

warn_list:
  - experimental

exclude_paths:
  - .cache/
  - .ansible_cache/

enable_list:
  - fqcn-builtins
